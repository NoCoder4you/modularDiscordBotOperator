# Platform (non-secret defaults)
MDBO_RUNTIME_ROOT=./runtime
MDBO_LOG_LEVEL=INFO

# Per-bot secrets: replace locally; never commit .env
CDA_ADMIN_TOKEN=replace_locally
CDA_PAY_TOKEN=replace_locally
UNBOT_TOKEN=replace_locally
RPA_ADMIN_TOKEN=replace_locally
